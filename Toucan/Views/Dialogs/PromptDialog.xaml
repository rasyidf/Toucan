<ui:FluentWindow
    x:Class="Toucan.PromptDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Toucan"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="450"
    Height="220"
    MinWidth="250"
    MinHeight="200"
    MaxWidth="600"
    MaxHeight="250"
    WindowBackdropType="Tabbed"
    WindowCornerPreference="Round"  
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="titleBar" Height="auto" />
            <RowDefinition x:Name="contentRow" Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="titleBarPrompt"
            Title="Add Translation Id"
            VerticalAlignment="Center"
            ShowMaximize="false"
            ShowMinimize="false" />
        <Grid
            Grid.Row="1"
            Margin="16,2,16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock
                x:Name="messageLabel"
                Margin="0,0,0,8"
                TextWrapping="Wrap"
                d:Text="Input" />
            <ui:TextBox
                x:Name="ResponseTextBox"
                Grid.Row="1" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <ui:Button Width="90" Content="Cancel" Margin="0,0,8,0" Click="CancelDialog"/>
                <ui:Button Width="90" Content="OK" Appearance="Primary" Click="OKButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
