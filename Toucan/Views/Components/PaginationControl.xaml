<UserControl x:Class="Toucan.Views.Components.PaginationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Toucan.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="600">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Button Command="{Binding FirstPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" IsEnabled="{Binding HasPreviousPage, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="2" ToolTip="First">
            <ui:SymbolIcon Symbol="ChevronDoubleLeft20"/>
        </Button>
        <Button Command="{Binding PreviousPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" IsEnabled="{Binding HasPreviousPage, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="2" ToolTip="Previous">
            <ui:SymbolIcon Symbol="ChevronLeft20"/>
        </Button>

        <ItemsControl ItemsSource="{Binding PageButtons, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="4,0" HorizontalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <Button Command="{Binding GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Number}" Width="32">
                                                <TextBlock Text="{Binding Number, FallbackValue=0}" VerticalAlignment="Stretch" HorizontalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                                <Setter Property="FontWeight" Value="Bold" />
                                                                <Setter Property="IsEnabled" Value="False" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsEllipsis}" Value="True">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <TextBlock Text="..." VerticalAlignment="Center" Margin="6" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Command="{Binding NextPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" IsEnabled="{Binding HasNextPage, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="2" ToolTip="Next">
            <ui:SymbolIcon Symbol="ChevronRight20" />
        </Button>
        <Button Command="{Binding LastPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" IsEnabled="{Binding HasNextPage, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="2" ToolTip="Last">
            <ui:SymbolIcon Symbol="ChevronDoubleRight20"/> 
        </Button>

        <TextBlock Text="{Binding PageMessage, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center" Margin="8,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <Button Content="Show All" Command="{Binding ShowAllCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" IsEnabled="{Binding IsPartial, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="6,0" />
    </StackPanel>
</UserControl>
