<UserControl
    x:Class="Toucan.Views.InspectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:loc="clr-namespace:Toucan.Locales"
    xmlns:c="clr-namespace:Toucan.Converters"
    xmlns:services="clr-namespace:Toucan.Services"
    xmlns:views="clr-namespace:Toucan.Views"
    xmlns:viewmodels="clr-namespace:Toucan.ViewModels"
    x:Name="InspectorPanel"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
    d:DesignHeight="500"
    d:DesignWidth="280"
    mc:Ignorable="d">

    <UserControl.Resources>
        <c:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Panel Header -->
        <Border Padding="10,4,6,4" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold"
                           Text="{x:Static loc:Strings.Panel_Inspector}" />
                <ui:Button Grid.Column="1" Padding="4" Appearance="Transparent"
                           Command="{Binding Source={x:Static services:PanelService.Instance}, Path=ToggleInspectorCommand}">
                    <ui:SymbolIcon FontSize="11" Symbol="Dismiss16" />
                </ui:Button>
            </Grid>
        </Border>

        <!-- Styled TabControl -->
        <TabControl x:Name="InspectorTabs" Grid.Row="1" BorderThickness="0" Padding="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="10,6"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="TabBorder" Padding="{TemplateBinding Padding}"
                                        Background="Transparent" BorderThickness="0,0,0,2"
                                        BorderBrush="Transparent" Cursor="Hand">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                            <Condition Property="IsSelected" Value="False"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Stats Tab -->
            <TabItem Header="{x:Static loc:Strings.Inspector_Stats}">
                <views:LanguagesView Margin="0"/>
            </TabItem>

            <!-- Suggestions Tab -->
            <TabItem Header="{x:Static loc:Strings.Inspector_Suggestions}">
                <Grid Margin="8,6">
                    <!-- Empty state -->
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                               FontSize="11" Opacity="0.5"
                               Text="{x:Static loc:Strings.Inspector_SelectKeySuggestions}"
                               Visibility="{Binding HasSelectedKeyDetails, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />

                    <!-- Content -->
                    <StackPanel Visibility="{Binding HasSelectedKeyDetails, Converter={StaticResource BoolToVis}}">
                        <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis" Margin="0,0,0,4"
                                   Text="{Binding SelectedKeyNamespace, StringFormat='Suggestions for: {0}'}"/>
                        <ItemsControl ItemsSource="{Binding Suggestions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                            CornerRadius="3" Padding="8,5" Margin="0,0,0,3">
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock FontSize="11" Opacity="0.5" Margin="0,4,0,0"
                                   Text="{x:Static loc:Strings.Inspector_NoSuggestions}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Suggestions.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Details Tab -->
            <TabItem Header="{x:Static loc:Strings.Inspector_Details}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="8,6">
                    <StackPanel>
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"
                                   FontSize="11" Opacity="0.5"
                                   Text="{x:Static loc:Strings.Inspector_SelectKey}"
                                   Visibility="{Binding HasSelectedKeyDetails, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}" />

                        <StackPanel Visibility="{Binding HasSelectedKeyDetails, Converter={StaticResource BoolToVis}}">
                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Key ID"/>
                            <TextBlock FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap"
                                       Text="{Binding SelectedKeyNamespace}" Margin="0,0,0,10"/>

                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Comment"/>
                            <TextBlock FontSize="12" TextWrapping="Wrap" Margin="0,0,0,10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding SelectedKeyComment}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedKeyComment}" Value="">
                                                <Setter Property="Text" Value="(none)" />
                                                <Setter Property="Opacity" Value="0.5" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Languages"/>
                            <ItemsControl ItemsSource="{Binding SelectedKeyLanguages}" Margin="0,2,0,10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock FontSize="11" Text="{Binding}" Margin="0,1"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Audit"/>
                            <TextBlock FontSize="12" TextWrapping="Wrap" Text="{Binding SelectedKeyAuditInfo}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Validation Tab -->
            <TabItem Header="Validation">
                <Grid Margin="8,6">
                    <!-- Empty state -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock FontSize="11" Opacity="0.5" Text="No validation issues"
                                   HorizontalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ValidationWarnings.Count, FallbackValue=0}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <!-- Warning cards -->
                    <ItemsControl ItemsSource="{Binding ValidationWarnings, FallbackValue={x:Null}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                        CornerRadius="3" Padding="8,5" Margin="0,0,0,3">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
